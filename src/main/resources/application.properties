server.port=@app.port@
server.servlet.context-path=@app.path@
server.workers.amount=1
management.endpoints.web.exposure.include=*
management.endpoint.shutdown.enabled=true
endpoints.shutdown.enabled=true

